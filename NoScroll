### h3 This will completely disable scrolling:

$('html, body').css({
    overflow: 'hidden',
    height: '100%'
});

### h3 To restore:

$('html, body').css({
    overflow: 'auto',
    height: 'auto'
});
